<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
	android:orientation="vertical"
>
	<!-- Following list view is dynamically populated with results -->
	<ListView
		android:id="@+id/attendee_search_advanced_list"
		android:layout_height="fill_parent"
		android:layout_width="fill_parent"
		android:divider="#00000000"
		android:visibility="gone"
	/>
	<TextView
	    android:id="@+id/attendee_search_advanced_message"
	    android:layout_width="fill_parent"
	    android:layout_height="fill_parent"
	    android:gravity="center"
	    android:text="@string/attendee_advanced_search_no_results"
	    android:textAppearance="@style/ListCellSubHeaderText"
	    android:textSize="18sp"
	    android:visibility="visible"
	/>
</LinearLayout>
